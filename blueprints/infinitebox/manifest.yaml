schema: blueprints/v0.1
version: v0.1
name: infinitebox
image: hub.helio.dev:4567/helio/runner/infinitebox:latest
cmd:
  - '/bin/sh -c i=0; while true; do echo "$i: $(date)"; i=$((i+1)); sleep 10; done'